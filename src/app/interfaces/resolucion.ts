export interface Resolucion {
    fechaEmision: string,
    inicioVigencia: string,
    finVigencia: string,
    ambito: string,
    estado: string
    //Descomentar cuando se seteen los datos desde el backend
   
}