export interface Empresas {
    ruc: string,
    descripcion: string,
    estado : string,
    estadoRutaAutorizada: string
   
    //Descomentar cuando se seteen los datos desde el backend
   //idempresa: number
}